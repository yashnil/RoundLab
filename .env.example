# Frontend
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Backend
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# Auth: modern projects verify tokens via JWKS (derived from SUPABASE_URL).
# Override only if needed:
SUPABASE_JWKS_URL=
SUPABASE_JWT_ISSUER=
# Legacy HS256 projects only: set the secret AND enable the fallback.
SUPABASE_JWT_SECRET=
AUTH_ALLOW_HS256_FALLBACK=false
DATABASE_URL=